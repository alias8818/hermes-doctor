# Provider fixture: provider configured but API key env var is not set (Hermes v24 format)
_config_version: 24
profile: default

model:
  default: claude-opus-4-20250514
  provider: auto

providers:
  anthropic:
    api_key_env: ANTHROPIC_API_KEY
  openai:
    api_key_env: OPENAI_API_KEY

terminal:
  backend: local

mcp_servers: {}

memory:
  dir: memory
  limit_mb: 10

skills:
  dir: skills

plugins: []
