# Provider fixture: auth.json has different active_provider than config (Hermes v24 format)
_config_version: 24
profile: default

model:
  default: claude-sonnet-4-20250514
  provider: auto

providers:
  anthropic:
    api_key_env: ANTHROPIC_API_KEY
  openai:
    api_key_env: OPENAI_API_KEY

models:
  - name: claude-sonnet-4-20250514
    provider: anthropic
  - name: gpt-4
    provider: openai

terminal:
  backend: local

mcp_servers: {}

memory:
  dir: memory
  limit_mb: 10

skills:
  dir: skills

plugins: []
